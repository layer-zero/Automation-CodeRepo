---
- name: Apply configlets to devices in CloudVision
  hosts: cvp1
  gather_facts: false
  vars_files:
    - ../vars/CVP_model.yml

  tasks:
    - name: Attach configlets to devices
      arista.cvp.cv_device_v3:
        devices: "{{ CVP_DEVICES }}"
        state: present
        apply_mode: loose
